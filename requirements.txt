# Needs to be fixed 🚧
# Optimized for local macOS (M2) environment

# --- Core Physics & Math ---
mujoco>=3.0.0
numpy>=2.0.0
pyyaml
msgpack

# --- Networking & Server (ZMQ) ---
pyzmq
rembg

# --- Training & Experiment Tracking ---
wandb>=0.16.0
tqdm>=4.66.0
lerobot==0.4.3
transformers<5.0.0
torch>=2.2.0
einops
huggingface-hub
scikit-learn      # For PCA projections
matplotlib        # For plotting clouds

# --- Diagnostics & Visuals ---
# Matches the current dashboard configuration
rerun-sdk[all]==0.28.2
av                # Stable video backend for macOS
pillow
imageio[ffmpeg]
